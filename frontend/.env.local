# Local dev only — backend on localhost:3000 (see backend-api.ts).
# Do NOT set the ALB URL here; production uses https://<cloudfront-domain>/api via same-origin routing.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api